services:
  avatar-job:
    build: .
    env_file:
      - .env
    environment:
      TZ: ${TZ:-UTC}
    volumes:
      - ./assets/images:/app/assets/images:ro
      - ./prompts.json:/app/prompts.json:ro
      - ./output:/app/output:rw
